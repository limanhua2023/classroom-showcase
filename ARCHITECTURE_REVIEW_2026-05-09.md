# ClassShow 架构审查与整改记录

## 当前架构

- 前端：静态 HTML/CSS/JS 页面，面向学生画廊、上传、详情、大屏、教师后台。
- 后端：Render Node/Express 单服务，负责鉴权、上传、评分、评论、归档、转码、教师运维接口。
- 数据库：Supabase Postgres，保存活动、用户、作品、评分、浏览、反馈、名单、回收区状态。
- 媒体：Supabase Storage 作为热存储，Google Drive/S3 作为归档层；视频转码与归档由后台轮询任务处理。

## 主要风险

- 数据库升级依赖手动运行 `upgrade_v4.sql`，若代码先上线但 SQL 未运行，新功能会进入降级模式。
- Render 免费实例有冷启动和单实例资源限制，教师后台重型扫描需要继续保持缓存与手动触发。
- 媒体链路同时依赖 Supabase Storage、manifest、归档副本和公开 URL，任何一个环节漂移都可能出现缩略图或主文件缺失。
- 当前仍有历史页面代码较长、部分旧函数重复覆盖的问题，后续应逐步模块化。
- 学生学习行为此前没有独立遥测表，无法做在线时长、活跃榜、小组战力和课堂粘性分析。

## 本轮整改

- 已创建可回滚还原点：`restore-2026-05-09-pre-engagement-upgrade`。
- 新增 `student_learning_sessions` 表，用于记录学生端有效在线学习时长。
- 新增学生心跳接口：`POST /api/student/learning/heartbeat`。
- 新增学生学习力接口：`GET /api/student/learning/summary`。
- 教师后台摘要增加 `learning_engagement` 数据，后续可继续做教师端班级活跃监控。
- 学生画廊新增“学习力竞技场”，展示个人在线时长、个人排名、学习力分数、小组排名、个人榜和小组榜。
- 学生上传页、作品详情页也会发送在线学习心跳，避免只有画廊停留才计时。
- 新功能做了数据库未升级降级处理：SQL 未运行时，不影响上传、浏览、评分、评论，只提示学习时长模块待升级。

## 后续建议

- 把 `upgrade_v4.sql` 拆分成按日期编号的迁移文件，减少重复手动粘贴带来的漂移。
- 给教师后台增加学习力趋势图：最近 7 天在线时长、低活跃学生提醒、小组活跃度热力图。
- 对大屏增加“学习力冲榜”栏目，把作品竞争和小组竞争直接展示给全班。
- 增加每日自动备份检查：导出活动快照、媒体缺失报告、归档失败清单。
- 把学生端页面逐步改造成组件化前端，降低重复函数和编码显示问题带来的维护成本。
